export type MORAddressType = {
    network: string;
    address: string;
    abi: any;
    chainID: number;
    logoImg: string;
}

export type ContractBalanceType = {
    balance: number;
    network: string;
    logoImg: string;
}